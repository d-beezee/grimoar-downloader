{
  "name": "grimoar-downloader",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "tsnd --respawn src/index.ts",
    "build": "tsc --noEmit false && yarn copy",
    "copy": "cp -r src/*.html dist"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
